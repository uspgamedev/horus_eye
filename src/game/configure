if [ -e ./CMakeLists.base ] && [ -e ./update_cmake ] ; then
    rm CMakeCache.txt
    ./update_cmake $@
    cmake .
else
    echo "Erro: impossivel configurar! Verifique se os arquivos ./CMakeLists.base e ./update_cmake estao presentes."
fi
